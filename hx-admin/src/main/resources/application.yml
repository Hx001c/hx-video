server:
  port: 8980

spring:
  application:
    name: API-ADMIN
  cloud:
    nacos:
      server-addr: 192.168.170.129:8848
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.170.129:3306/hx?characterEncoding=UTF-8&useSSL=false
    username: root
    password: wcy011230
  #redis配置
  redis:
    host: 192.168.170.129
    port: 6378
  mvc:
    hiddenmethod:
      filter:
        enabled: true

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.athx.com.athx.entity
logging:
  level:
    com.athx.dao: debug

#jwt配置
jwt:
  tokenHeader: Authorization #JWT存储的请求头
  secret: hx-secret #JWT加解密使用的密钥
  expireTime: 30 #jwtToken的默认有效时间 单位分钟
  tokenHead: 'Bearer '  #JWT负载中拿到开头